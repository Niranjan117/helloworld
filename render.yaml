services:
  - type: web
    name: training-api
    runtime: java
    plan: free
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/training-api-1.0.0.jar
    envVars:
      - key: JAVA_VERSION
        value: "17"
